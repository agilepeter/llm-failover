# CLAUDE.md — llm-failover (OSS)

Public OSS LLM provider-failover chain.

**Project memory** is NOT in this repo — central Claude auto-memory topic file: `llm_provider_chain.md`. Key rule: the production chain in `automations/` is a TWIN of this OSS repo — keep both in sync when either changes.

Convention: "project memory" = central topic file · "project rules" = this file. Workspace rules: `~/saarvis/CLAUDE.md`.
